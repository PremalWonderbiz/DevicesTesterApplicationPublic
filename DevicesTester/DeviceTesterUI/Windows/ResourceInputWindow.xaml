<Window x:Class="DeviceTesterUI.Windows.ResourceInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Device Resources"
        Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Static Data -->
        <TextBlock Grid.Row="0" Text="Static resources :" Margin="0,0,0,5"/>
        <TextBox x:Name="StaticDataTextBox"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>

        <!-- Dynamic Data -->
        <TextBlock Grid.Row="2" Text="Dynamic resources :" Margin="0,10,0,5"/>
        <TextBox x:Name="DynamicDataTextBox"
                 Grid.Row="3"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" Width="80" Margin="5" IsCancel="True" Padding="0,5"/>
            <Button Content="Save" Width="80" Margin="5" IsDefault="True" Click="Save_Click" Padding="0,5"/>
        </StackPanel>
    </Grid>
</Window>
